services:
  postgres:
    image: postgres:17
    container_name: delta-postgres
    environment:
      POSTGRES_DB: delta
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"