package com.coderGtm.delta.attendance.entity;

/**
 * Supported attendance actions stored directly as an enum to keep the model
 * simple and aligned with the current domain.
 */
public enum AttendanceEntryType {
	CLOCK_IN,
	CLOCK_OUT
}
